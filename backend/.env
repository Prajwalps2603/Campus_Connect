PORT=5000
DB_USER=postgres
DB_PASSWORD=Prajwalmdh@123
DB_HOST=localhost
DB_PORT=5432
# Database name contains a space, keep it quoted so pg connection string parsing is reliable
DB_NAME="Campus Connect"

# JWT / Auth settings
JWT_SECRET=change_this_to_a_long_random_secret_value
JWT_EXPIRES_IN=2h