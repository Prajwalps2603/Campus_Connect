server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/campus_connect
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

app.jwt.secret=dev_fallback_secret_change_me
app.jwt.expires=7200000

# Static uploads directory (adjust as needed)
app.upload.dir=${user.dir}/uploads
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=50MB
